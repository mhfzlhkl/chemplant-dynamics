# Template Example

This folder contains the template session example for documentation and
developer onboarding. It's intentionally placed under `tests/examples` so
it does not get discovered as a runtime case plugin by the engine.
